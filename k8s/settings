# Note: Please do not change name of this file
# This filename is used as a key in the ConfigMap
# Your config goes here
# TODO: Find a better way to set projectId (hardcoded for now)
# Ref: https://docs.atlas.mongodb.com/api/#project-id
# This is project id for aya-staging MongoDb Atlas Project
projectId: "<MongoDB Atlas Project ID here>"
tags:
    mongodb_version:
      value: Cluster.MongoDBVersion
    id:
      value: Cluster.ID
    name:
      value: Cluster.Name
    aya_type:
      value:
        - if: Cluster.Name matches ".*-staging.*"
          then: "staging"
        - if: Cluster.Name matches  ".*-develop"
          then: "develop"